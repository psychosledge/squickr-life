{
  "name": "squickr-life",
  "version": "0.2.3",
  "private": true,
  "description": "Get shit done quicker with Squickr!",
  "keywords": ["task-tracker", "event-sourcing", "pwa", "offline-first"],
  "author": "Your Name",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter client dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r test --watch",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
